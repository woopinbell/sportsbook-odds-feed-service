> 생성물. 직접 수정 금지. 답: ../commits/002.md

# 002 — feat(provider): MockOddsProvider with deterministic event simulator

## 개요

`OddsProvider`의 **기본 구현체**. `@Profile("mock")`로 활성화되고, 외부 키 없이
로컬에서 전체 이벤트 lifecycle을 굴린다. 부하 테스트·통합 테스트·카오스
시나리오가 전부 이 위에서 돈다(ADR-0010 — mock 1차). 핵심은 **결정론
(deterministic)** — 같은 seed면 같은 결과라 테스트가 재현 가능하다.

## 작업 순서

1. `ApplicationConfig` — `@EnableScheduling` + `Clock` @Bean.
2. `MockProperties` — `oddsfeed.mock.*` 바인딩.
3. `OddsSimulator` — 순수 가격 random walk (Spring 무관).
4. `MockOddsProvider` — lifecycle 진행 + odds emit + 결과 합성.
5. 단위 테스트 — lifecycle, emit, 결과, 시뮬레이터 수렴/클램핑/결정성.

## 작업 내역

### OddsSimulator — 순수 함수로 분리

시그니처는 노출, 상수값과 walk 본문은 비운다:

```java
public final class OddsSimulator {

  // TODO: 노이즈 표준편차·평균회귀 비율·odds 하한/상한 상수 4개 (값 + 이름)
  static final double NOISE_STDDEV = ___;
  static final double MEAN_REVERSION = ___;
  static final double MIN_ODDS = ___;
  static final double MAX_ODDS = ___;

  private OddsSimulator() {}

  /** 신규 selection 초기 odds: 정확히 공정값(V1 margin 0). */
  public static Odds initialOdds(double impliedProbability) {
    // TODO: 공정값(1/p)을 Odds.SCALE로
  }

  public static Odds nextOdds(Odds current, double impliedProbability, Random rng) {
    // TODO: 곱셈 가우시안 노이즈로 흔들고 → fair(1/p) 쪽으로 일부 평균회귀
    //       → [MIN_ODDS, MAX_ODDS] clamp → Odds.SCALE/HALF_EVEN
  }
}
```

**책임:** 순수 random walk는 발산하거나 `Odds<1.0`로 예외 → fair 쪽으로 당기고
clamp해서 항상 유효한 odds. base 확률 HOME 0.45 / DRAW 0.25 / AWAY 0.30(합 1.0).

### MockOddsProvider — 상태 + 시간 진행 + emit

클래스 골격·필드·메서드 시그니처는 노출, 본문은 비운다:

```java
@Component
@Profile("mock")
public class MockOddsProvider implements OddsProvider {

  static final int INITIAL_EVENT_COUNT = ___;        // TODO
  static final Duration MATCH_DURATION_MOCK = ___;   // TODO
  static final Duration KICKOFF_SPACING_MOCK = ___;  // TODO
  private static final double SECONDS_PER_MINUTE = 60.0;

  private final MockProperties props;
  private final Clock clock;
  private final Map<EventId, MockEvent> events = new ConcurrentHashMap<>();
  private final Map<EventId, Sinks.Many<ProviderEvent>> streams = new ConcurrentHashMap<>();
  private final Map<EventId, MatchOutcome> outcomes = new ConcurrentHashMap<>();
  private Random rng;

  public MockOddsProvider(MockProperties props, Clock clock) { /* TODO */ }

  @PostConstruct
  void seed() {
    // TODO: rng 초기화(randomSeed==0이면 new Random()), INITIAL_EVENT_COUNT 만큼
    //       buildEvent(kickoff/end를 toRealDuration으로 배치) + multicast sink 등록
  }

  @Scheduled(fixedRateString = "${oddsfeed.mock.tick-interval-ms:500}")
  void scheduledTick() {
    // TODO: tick(clock.instant())
  }

  void tick(Instant now) {            // package-private — 테스트가 직접 호출
    // TODO: 모든 이벤트 advance
  }

  // 이 커밋의 심장.
  private void advance(MockEvent event, Instant now) {
    // TODO: FINISHED/CANCELLED면 정지;
    //       SCHEDULED & kickoff 도달 → transitionTo(IN_PLAY);
    //       IN_PLAY & end 도달 → transitionTo(FINISHED) + outcome 저장 후 return;
    //       그 외엔 OPEN 마켓의 각 selection을 nextOdds로 갱신, 변하면 OddsUpdated emit.
  }

  private void transitionTo(MockEvent event, EventLifecycleStatus next, Instant now) {
    // TODO: status 갱신 + 불변 EventSummary 새로 생성(status만 교체) + LifecycleUpdated emit
  }
  private void emit(EventId eventId, ProviderEvent event) {
    // TODO: 해당 sink에 tryEmitNext (sink 없으면 무시)
  }
  private MatchOutcome synthesizeOutcome(MockEvent event, Instant now) {
    // TODO: base 확률로 roll → score 결정, COMPLETED, (V1) detail
  }
  private Duration toRealDuration(Duration mockDuration) {
    // TODO: mock분 → 실초 변환(minutesPerSecond로 압축). 60 이중 곱 버그 주의.
  }

  @Override public List<EventSummary> listEvents(Sport sport) { /* TODO: sport 필터 + List.copyOf */ }
  @Override public Flux<ProviderEvent> streamEvents(EventId eventId) { /* TODO: sink.asFlux 또는 Flux.empty */ }
  @Override public Optional<MatchOutcome> getMatchResult(EventId eventId) { /* TODO */ }

  static final class MockEvent {     // TODO: summary, markets, kickoffAt, endAt, status
  }
  static final class MockMarket {    // TODO: marketId, type, status(기본 OPEN), selections
  }
  static final class MockSelection { // TODO: selectionId, name, impliedProbability, currentOdds
  }
}
```

**책임:** `Clock` 주입으로 스케줄러 없이 `tick(Instant)` 직접 호출해 테스트.
`advance`가 lifecycle 상태머신 + 가격 엔진. odds는 OPEN 마켓에만 흐르고 변화가
있을 때만 emit.

### V1 scope + 설정 바인딩

설정 record는 계약이라 노출, Clock @Bean 본문만 비운다:

```java
@ConfigurationProperties(prefix = "oddsfeed.mock")
public record MockProperties(
    double minutesPerSecond, Scenarios scenarios,
    double baseHomeWinProbability, double baseDrawProbability, double baseAwayWinProbability,
    long randomSeed, int tickIntervalMs) {
  public record Scenarios(boolean autoRotate, int rotationIntervalSeconds) {}
}

@Configuration
@EnableScheduling
@EnableConfigurationProperties(MockProperties.class)
public class ApplicationConfig {
  @Bean
  public Clock systemClock() {
    // TODO: system UTC
  }
}
```

**책임:** V1은 이벤트당 `MATCH_RESULT_1X2` 마켓 1개 + selection 3종. `scenarios`
플래그는 미리 둬서 003이 이 클래스 모양을 안 건드리게.

## 검증

- `./mvnw test -Dtest=OddsSimulatorTest,MockOddsProviderTest` 통과.
  - `OddsSimulatorTest`: 클램핑 범위, 평균회귀 수렴, 동일 seed 결정성.
  - `MockOddsProviderTest`: 3경기 seed, full lifecycle 전이, FINISHED 시
    `MatchOutcome` 생성, odds emit.
