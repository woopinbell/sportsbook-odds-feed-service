> 생성물. 직접 수정 금지. 답: ../commits/005.md

# 005 — feat(cache): Redis write-through cache for current odds and metadata

## 개요

하류 서비스가 Kafka 업데이트 **사이사이에** 읽는 거울(mirror). betting-service는
슬립 검증 때 현재 odds를, gateway는 WebSocket hello 프레임용 이벤트 메타데이터를,
admin-api는 마켓 상태를 본다. 셋 다 HTTP로 우리에게 묻는 대신 **Redis를 read**해서,
업데이트 사이 우리 가용성에 안 묶이게 한다(repo CLAUDE.md write-through 결정).

## 작업 순서

1. `CacheProperties` — `oddsfeed.cache.ttl` 바인딩.
2. `CacheKeys` — key 문자열 생성 중앙화.
3. `RedisOddsCache` — store/get 3쌍.
4. `ApplicationConfig`에 `CacheProperties` 등록.
5. Testcontainers Redis 통합 테스트.

## 작업 내역

### key 구조 (CLAUDE.md 결정, CacheKeys에 박제)

key 레이아웃(좌변)은 구조이므로 노출, 저장 형식(우변)과 조립 본문은 비운다:

```
odds:{eventId}:{marketId}:{selectionId}  → ?   # TODO: 무슨 형식?
event:{eventId}                          → ?   # TODO
market:{eventId}:{marketId}              → ?   # TODO
```

```java
public final class CacheKeys {
  private CacheKeys() {}
  public static String odds(EventId eventId, MarketId marketId, SelectionId selectionId) {
    // TODO
  }
  public static String event(EventId eventId) { /* TODO */ }
  public static String market(EventId eventId, MarketId marketId) { /* TODO */ }
}

@ConfigurationProperties(prefix = "oddsfeed.cache")
public record CacheProperties(Duration ttl) {}
```

**책임:** key 생성을 한 곳에 모아 rename이 단일 파일 변경. 단일 24h TTL(V1).

### 저장 포맷 결정

시그니처는 노출, 각 포맷의 직렬화/역직렬화 본문은 비운다:

```java
// odds — betting-service hot path
public void storeOdds(EventId e, MarketId m, SelectionId s, Odds odds) { /* TODO */ }
public Optional<Odds> getOdds(EventId e, MarketId m, SelectionId s) { /* TODO */ }

// event — 중첩 구조(sport/status/kickoff)
public void storeEvent(EventSummary summary) { /* TODO: 직렬화 + 예외 래핑 */ }
public Optional<EventSummary> getEvent(EventId eventId) { /* TODO */ }

// market status (ordinal로 저장하지 말 것)
public void storeMarketStatus(EventId e, MarketId m, MarketStatus status) { /* TODO */ }
public Optional<MarketStatus> getMarketStatus(EventId e, MarketId m) { /* TODO */ }
```

**책임:** odds=평문 decimal 문자열(hot path, `Odds.SCALE` 보존), `EventSummary`=JSON
(중첩), `MarketStatus`=enum **이름**(ordinal은 순서 변경에 깨짐, redis-cli 가독).

### write-through 정직함

```java
@Component
public class RedisOddsCache {
  private final StringRedisTemplate redis;
  private final ObjectMapper objectMapper;
  private final Duration ttl;

  public RedisOddsCache(StringRedisTemplate redis, ObjectMapper objectMapper, CacheProperties props) {
    // TODO
  }
  // ... store/get 3쌍 (위)
}
```

**책임:** orchestrator(007)가 publisher→cache 순으로 쓴다. non-atomic이지만 odds
이벤트의 **멱등성**(다음 변경이 정정)으로 정당화. 진짜 원자성(transactional
outbox)은 돈을 다루는 wallet/betting의 몫(ADR-0006)이라 여기선 세금 안 냄.

## 검증

- `./mvnw test -Dtest=RedisOddsCacheTest` 통과 (Docker 필요 — Testcontainers Redis).
  - 3 round-trip(odds decimal / EventSummary JSON / MarketStatus enum) + missing-key
    empty + CacheKeys 포맷 잠금 테스트.
